export const PALETTE = [
  { name: 'red',       h: 0,   s: 72, l: 68 },
  { name: 'red',       h: 0,   s: 72, l: 60 },
  { name: 'red',       h: 0,   s: 72, l: 52 },
  { name: 'red',       h: 0,   s: 72, l: 44 },
  { name: 'scarlet',   h: 6,   s: 72, l: 68 },
  { name: 'scarlet',   h: 6,   s: 65, l: 60 },
  { name: 'scarlet',   h: 6,   s: 55, l: 52 },
  { name: 'scarlet',   h: 6,   s: 44, l: 44 },
  { name: 'crimson',   h: 12,  s: 50, l: 68 },
  { name: 'crimson',   h: 12,  s: 50, l: 60 },
  { name: 'crimson',   h: 12,  s: 50, l: 52 },
  { name: 'crimson',   h: 12,  s: 50, l: 44 },
  { name: 'tangerine', h: 32,  s: 72, l: 68 },
  { name: 'tangerine', h: 32,  s: 65, l: 60 },
  { name: 'tangerine', h: 32,  s: 55, l: 52 },
  { name: 'tangerine', h: 32,  s: 44, l: 44 },
  { name: 'orange',    h: 25,  s: 78, l: 68 },
  { name: 'orange',    h: 25,  s: 78, l: 60 },
  { name: 'orange',    h: 25,  s: 78, l: 52 },
  { name: 'orange',    h: 25,  s: 78, l: 44 },
  { name: 'amber',     h: 37,  s: 50, l: 68 },
  { name: 'amber',     h: 37,  s: 50, l: 60 },
  { name: 'amber',     h: 37,  s: 50, l: 52 },
  { name: 'amber',     h: 37,  s: 50, l: 44 },
  { name: 'gold',      h: 55,  s: 68, l: 68 },
  { name: 'gold',      h: 55,  s: 65, l: 60 },
  { name: 'gold',      h: 55,  s: 55, l: 52 },
  { name: 'gold',      h: 55,  s: 44, l: 44 },
  { name: 'yellow',    h: 48,  s: 68, l: 68 },
  { name: 'yellow',    h: 48,  s: 68, l: 60 },
  { name: 'yellow',    h: 48,  s: 68, l: 52 },
  { name: 'yellow',    h: 48,  s: 68, l: 44 },
  { name: 'chartr.',   h: 62,  s: 50, l: 68 },
  { name: 'chartr.',   h: 62,  s: 50, l: 60 },
  { name: 'chartr.',   h: 62,  s: 50, l: 52 },
  { name: 'chartr.',   h: 62,  s: 50, l: 44 },
  { name: 'lime',      h: 75,  s: 48, l: 68 },
  { name: 'lime',      h: 75,  s: 48, l: 60 },
  { name: 'lime',      h: 75,  s: 48, l: 52 },
  { name: 'lime',      h: 75,  s: 48, l: 44 },
  { name: 'fern',      h: 95,  s: 50, l: 68 },
  { name: 'fern',      h: 95,  s: 50, l: 60 },
  { name: 'fern',      h: 95,  s: 50, l: 52 },
  { name: 'fern',      h: 95,  s: 50, l: 44 },
  { name: 'sage',      h: 108, s: 46, l: 68 },
  { name: 'sage',      h: 108, s: 52, l: 60 },
  { name: 'sage',      h: 108, s: 60, l: 52 },
  { name: 'sage',      h: 108, s: 66, l: 44 },
  { name: 'green',     h: 120, s: 42, l: 68 },
  { name: 'green',     h: 120, s: 42, l: 60 },
  { name: 'green',     h: 120, s: 42, l: 52 },
  { name: 'green',     h: 120, s: 42, l: 44 },
  { name: 'juniper',   h: 150, s: 46, l: 68 },
  { name: 'juniper',   h: 150, s: 52, l: 60 },
  { name: 'juniper',   h: 150, s: 60, l: 52 },
  { name: 'juniper',   h: 150, s: 66, l: 44 },
  { name: 'pine',      h: 140, s: 50, l: 68 },
  { name: 'pine',      h: 140, s: 50, l: 60 },
  { name: 'pine',      h: 140, s: 50, l: 52 },
  { name: 'pine',      h: 140, s: 50, l: 44 },
  { name: 'teal',      h: 158, s: 46, l: 68 },
  { name: 'teal',      h: 158, s: 46, l: 60 },
  { name: 'teal',      h: 158, s: 46, l: 52 },
  { name: 'teal',      h: 158, s: 46, l: 44 },
  { name: 'aqua',      h: 186, s: 46, l: 68 },
  { name: 'aqua',      h: 186, s: 52, l: 60 },
  { name: 'aqua',      h: 186, s: 60, l: 52 },
  { name: 'aqua',      h: 186, s: 66, l: 44 },
  { name: 'cyan',      h: 178, s: 50, l: 68 },
  { name: 'cyan',      h: 178, s: 50, l: 60 },
  { name: 'cyan',      h: 178, s: 50, l: 52 },
  { name: 'cyan',      h: 178, s: 50, l: 44 },
  { name: 'sky',       h: 195, s: 58, l: 68 },
  { name: 'sky',       h: 195, s: 58, l: 60 },
  { name: 'sky',       h: 195, s: 58, l: 52 },
  { name: 'sky',       h: 195, s: 58, l: 44 },
  { name: 'denim',     h: 213, s: 46, l: 68 },
  { name: 'denim',     h: 213, s: 52, l: 60 },
  { name: 'denim',     h: 213, s: 60, l: 52 },
  { name: 'denim',     h: 213, s: 66, l: 44 },
  { name: 'cerulean',  h: 207, s: 50, l: 68 },
  { name: 'cerulean',  h: 207, s: 50, l: 60 },
  { name: 'cerulean',  h: 207, s: 50, l: 52 },
  { name: 'cerulean',  h: 207, s: 50, l: 44 },
  { name: 'blue',      h: 220, s: 62, l: 68 },
  { name: 'blue',      h: 220, s: 62, l: 60 },
  { name: 'blue',      h: 220, s: 62, l: 52 },
  { name: 'blue',      h: 220, s: 62, l: 44 },
  { name: 'cobalt',    h: 232, s: 46, l: 68 },
  { name: 'cobalt',    h: 232, s: 52, l: 60 },
  { name: 'cobalt',    h: 232, s: 60, l: 52 },
  { name: 'cobalt',    h: 232, s: 66, l: 44 },
  { name: 'indigo',    h: 243, s: 50, l: 68 },
  { name: 'indigo',    h: 243, s: 50, l: 60 },
  { name: 'indigo',    h: 243, s: 50, l: 52 },
  { name: 'indigo',    h: 243, s: 50, l: 44 },
  { name: 'violet',    h: 265, s: 55, l: 68 },
  { name: 'violet',    h: 265, s: 55, l: 60 },
  { name: 'violet',    h: 265, s: 55, l: 52 },
  { name: 'violet',    h: 265, s: 55, l: 44 },
  { name: 'grape',     h: 275, s: 46, l: 68 },
  { name: 'grape',     h: 275, s: 52, l: 60 },
  { name: 'grape',     h: 275, s: 60, l: 52 },
  { name: 'grape',     h: 275, s: 66, l: 44 },
  { name: 'plum',      h: 285, s: 50, l: 68 },
  { name: 'plum',      h: 285, s: 50, l: 60 },
  { name: 'plum',      h: 285, s: 50, l: 52 },
  { name: 'plum',      h: 285, s: 50, l: 44 },
  { name: 'magenta',   h: 320, s: 50, l: 68 },
  { name: 'magenta',   h: 320, s: 50, l: 60 },
  { name: 'magenta',   h: 320, s: 50, l: 52 },
  { name: 'magenta',   h: 320, s: 50, l: 44 },
  { name: 'blush',     h: 330, s: 65, l: 68 },
  { name: 'blush',     h: 330, s: 65, l: 60 },
  { name: 'blush',     h: 330, s: 55, l: 52 },
  { name: 'blush',     h: 330, s: 44, l: 44 },
  { name: 'rose',      h: 340, s: 65, l: 68 },
  { name: 'rose',      h: 340, s: 65, l: 60 },
  { name: 'rose',      h: 340, s: 65, l: 52 },
  { name: 'rose',      h: 340, s: 65, l: 44 },
];

/**
 * Parse the hue (0–360) from a colour string.
 * Handles hsl(...) strings and 3/6-digit hex values.
 */
function parseHue(colour) {
  if (!colour) return null;

  // HSL string: hsl(h, s%, l%) or hsl(h s% l%)
  const hslMatch = colour.match(/hsl\(\s*([\d.]+)/i);
  if (hslMatch) return parseFloat(hslMatch[1]);

  // Hex: #rgb or #rrggbb
  const hex = colour.replace('#', '');
  let r, g, b;
  if (hex.length === 3) {
    r = parseInt(hex[0] + hex[0], 16) / 255;
    g = parseInt(hex[1] + hex[1], 16) / 255;
    b = parseInt(hex[2] + hex[2], 16) / 255;
  } else if (hex.length === 6) {
    r = parseInt(hex.slice(0, 2), 16) / 255;
    g = parseInt(hex.slice(2, 4), 16) / 255;
    b = parseInt(hex.slice(4, 6), 16) / 255;
  } else {
    return null;
  }

  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  const delta = max - min;
  if (delta === 0) return 0;

  let h;
  if (max === r) h = ((g - b) / delta) % 6;
  else if (max === g) h = (b - r) / delta + 2;
  else h = (r - g) / delta + 4;

  h = (h * 60 + 360) % 360;
  return h;
}

/** Circular distance between two hues on the 0–360 wheel. */
function hueDistance(a, b) {
  const diff = Math.abs(a - b) % 360;
  return diff > 180 ? 360 - diff : diff;
}

/**
 * Returns the lightness preference weight for scoring.
 * Lower is better (picked first).
 *   l:60 → 0, l:52 → 0, l:44 → 1, l:68 → large penalty
 */
function lightnessWeight(l) {
  if (l === 68) return 1000;
  if (l === 44) return 1;
  return 0; // 60 and 52 are equally preferred
}

/**
 * Pick a colour from PALETTE that maximises hue distance from all
 * existing project colours.
 *
 * @param {Array<{colour?: string, color?: string}>} existingProjects
 * @param {Array<{colour?: string, color?: string}>} [avoidAlso=[]] - additional colours to avoid (e.g. custom chips)
 * @returns {string} HSL colour string
 */
export function pickProjectColour(existingProjects, avoidAlso = []) {
  const existingHues = [...(existingProjects ?? []), ...(avoidAlso ?? [])]
    .map((p) => parseHue(p.colour ?? p.color))
    .filter((h) => h !== null);

  // Score each palette entry: primary key = min hue distance (higher is better),
  // tie-break = lightness weight (lower is better).
  let best = null;
  let bestMinDist = -Infinity;
  let bestLWeight = Infinity;

  for (const entry of PALETTE) {
    const lw = lightnessWeight(entry.l);

    let minDist;
    if (existingHues.length === 0) {
      minDist = 360; // No existing colours — all entries equally "distant"
    } else {
      minDist = Math.min(...existingHues.map((eh) => hueDistance(entry.h, eh)));
    }

    // Apply lightness penalty: treat l:68 entries as if their min distance is
    // capped at (real minDist - 1000), which pushes them below any l:60/52/44
    // entry unless all other lightness levels are already exhausted.
    const effectiveDist = minDist - lw;

    if (
      effectiveDist > bestMinDist ||
      (effectiveDist === bestMinDist && lw < bestLWeight)
    ) {
      best = entry;
      bestMinDist = effectiveDist;
      bestLWeight = lw;
    }
  }

  return `hsl(${best.h}, ${best.s}%, ${best.l}%)`;
}

/**
 * Pick a colour for a custom chip, preferring l:68 (light/pastel) entries
 * that are deprioritised for projects. Falls back to l:60/52 then l:44.
 *
 * @param {Array<{colour?: string, color?: string}>} existingChips
 * @param {Array<{colour?: string, color?: string}>} [avoidAlso=[]] - additional colours to avoid (e.g. staged projects)
 * @returns {string} HSL colour string
 */
export function pickCustomChipColour(existingChips, avoidAlso = []) {
  const existingHues = [...(existingChips ?? []), ...(avoidAlso ?? [])]
    .map((p) => parseHue(p.colour ?? p.color))
    .filter((h) => h !== null);

  // Inverted lightness weight: l:68 is most preferred (weight 0),
  // l:60/52 are next (weight 1), l:44 is last (weight 2).
  function chipLightnessWeight(l) {
    if (l === 68) return 0;
    if (l === 60 || l === 52) return 1;
    return 2; // l:44
  }

  let best = null;
  let bestMinDist = -Infinity;
  let bestLWeight = Infinity;

  for (const entry of PALETTE) {
    const lw = chipLightnessWeight(entry.l);

    let minDist;
    if (existingHues.length === 0) {
      minDist = 360;
    } else {
      minDist = Math.min(...existingHues.map((eh) => hueDistance(entry.h, eh)));
    }

    // Penalty of 1000 per lightness tier ensures l:68 exhausted before l:60/52,
    // and l:60/52 exhausted before l:44.
    const effectiveDist = minDist - lw * 1000;

    if (
      effectiveDist > bestMinDist ||
      (effectiveDist === bestMinDist && lw < bestLWeight)
    ) {
      best = entry;
      bestMinDist = effectiveDist;
      bestLWeight = lw;
    }
  }

  return `hsl(${best.h}, ${best.s}%, ${best.l}%)`;
}
